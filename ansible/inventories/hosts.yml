all:
  hosts:
    vps:
      ansible_host: "{{ lookup('env', 'VPS_HOST') }}"
      ansible_user: xcraftman52
      ansible_become_method: sudo    
    docker-1:
      ansible_host: "10.32.100.42"
      ansible_user: xcraftman52
      ansible_become_method: sudo